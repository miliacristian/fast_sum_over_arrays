#include "utility.h"
int** generate_random_integer_matrix(int num_rows,int num_cols){
	//suppose that matrix contains random number
	int**matrix=alloc_matrix_int_and_materialize(num_rows,num_cols);
	return matrix;
}